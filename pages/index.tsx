import { ContentList } from '@/components/modules';

const Home = () => {
  return (
    <div className="w-full h-full phone:overflow-hidden phone:px-3">
      <ContentList />
    </div>
  );
};

export default Home;
